export const EMAIL_REGEX = /^[a-zA-Z0-9._%+-]+@gmail\.com$/;
export const EMAIL_REGEX_GENERIC = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const NAME_REGEX = /^[A-Za-z\s]+$/;

export const IMAGE_REGEX = /\.(jpg|jpeg|png|gif|webp)$/i;

export const VIDEO_REGEX = /\.(mp4|webm|ogg|mov)$/i;
